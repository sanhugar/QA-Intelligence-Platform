# Prompts

Versioned prompt assets for AI capabilities.

**Status:** Empty. Do not add prompts until product/architecture authorizes AI generation work.

Future convention:

```
prompts/<capability>/v<n>/system.md
prompts/<capability>/v<n>/user.md
prompts/<capability>/v<n>/metadata.json
```

Prompt versions become part of artifact lineage metadata.
