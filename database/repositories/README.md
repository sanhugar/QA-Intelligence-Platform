# Repository Standards

This folder documents naming, ownership, and query review practices.

- Interfaces: `apps/api/src/domain/repositories`
- Implementations: `apps/api/src/infrastructure/persistence`
- Do not create a second ORM layer here
