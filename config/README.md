# Config Templates

Checked-in environment templates and composition defaults.

- Commit `*.example` files only
- Real `.env` files are gitignored
- Validate config via `packages/config` when apps boot
